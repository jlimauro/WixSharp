This sample demonstrates how to deploy WebSite. .

Even though Wix# supports WixIISExtension (WiX) with IISVirtualDir(Wix#) class this code sample is demonstrates how to usilize WiX extensions if they are not natively supported by Wix#.
Execute corresponding .cmd file to build desired msi. Then execute the .msi to start the installation.